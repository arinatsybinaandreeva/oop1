#include <iostream>
#include <map>
using namespace std;
float A[10] = {};
int ind;
double n;
int main()
{
    setlocale(LC_ALL, "Rus");
    for (int i = 0; i < 10; i++)
    {
        double n;
        cin >> n;
        A[i] = n;
    }
    for (int i = 0; i < 10; i++)
    {
        cout<<(A[i])<<" ";
    }
    cout << (&A[0]) << " ";
    for (int i = 0; i < 10; i++)
    {
        cout << (A[i]) << " - находится на расстоянии "<<i<<" от начала массива, ";
    }
}
